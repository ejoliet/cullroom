interface:
  display_name: "Cullroom"
  short_description: "Repo-specific patterns and workflows for cullroom"
  default_prompt: "Use the cullroom repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true